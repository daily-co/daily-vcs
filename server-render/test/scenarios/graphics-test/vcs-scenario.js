const state = {
  activeVideoInputSlots: [true],
  params: {},
};

export default {
  compositionId: 'example:graphics-test',
  durationInFrames: 50,
  outputFrames: [49],
  initialState: { ...state },
};
